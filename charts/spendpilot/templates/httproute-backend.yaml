{{- if .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: spend-control-identity
  namespace: {{ include "business-ai-app.namespace" . }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}
  rules:
{{- range .Values.routes.identityPathPrefixes }}
    - matches:
        - path:
            type: PathPrefix
            value: {{ . | quote }}
      backendRefs:
        - name: spend-control-identity
          port: {{ $.Values.identityService.service.port }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: spend-control-finance
  namespace: {{ include "business-ai-app.namespace" . }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}
  rules:
{{- range .Values.routes.financePathPrefixes }}
    - matches:
        - path:
            type: PathPrefix
            value: {{ . | quote }}
      backendRefs:
        - name: spend-control-finance
          port: {{ $.Values.financeService.service.port }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: spend-control-documents
  namespace: {{ include "business-ai-app.namespace" . }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}
  rules:
{{- range .Values.routes.documentsPathPrefixes }}
    - matches:
        - path:
            type: PathPrefix
            value: {{ . | quote }}
      backendRefs:
        - name: spend-control-documents
          port: {{ $.Values.documentsService.service.port }}
{{- end }}
{{- end }}
